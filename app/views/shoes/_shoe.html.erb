<!-- Zapatilla -->
      <%= div_for shoe do %>
        
        <h4><%= link_to shoe.nombre, [@runner, shoe] %></h4>
        <h4><%= sprintf("%0.02f", shoe.totalkms) %></h4>
        
        <div class="acciones">
          <% if shoe.runner == current_runner %>
            <%= link_to 'Eliminar', [@runner, shoe], method: :delete, remote: true, data: { confirm: '¿Seguro?' }%>
          <% end %>   
        </div>
      <% end %>
<!-- Fin Zapatilla-->