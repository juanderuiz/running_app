<header>
<h3>Lista de sesiones de entrenamiento de <%= link_to @runner.name, @runner %></h3>
</header>

<table>
  <thead>
    <tr>
      <th>Fecha</th>
      <th>Distancia</th>
      <th>Tiempo</th>
      <th>Descripcion</th>
      <th>Comentarios</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @trainings.each do |training| %>
      <tr>
	<td><%= training.date %></td>
        <td><%= training.kms %></td>
        <td><%= training.time %></td>
	 <!--<td class="enlace"><%= link_to training.runner.name, training.runner %></td>
    <% corredor = Runner.find(training.runner_id)%> 
	<td><%= link_to corredor.name, corredor %></td> -->
        <td><%= training.description %></td>
        <td><%= training.comments %></td>
        <td class="enlace ver"><%= link_to 'Ver', [@runner, training] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Training', new_runner_training_path(@runner) %>
